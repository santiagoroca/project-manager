<!DOCTYPE html>
<html>

    <head>
        {{{#head}}}
            {{{head}}}
        {{{/head}}}
    </head>

    <body>
        {{{#body}}}
            {{{body}}}
        {{{/body}}}
    </body>

    <script>

        var domain = location.origin;

        Promise.all([
            new Promise (function (resolve, reject) {
                var script = document.createElement('script');
                script.type = "text\/javascript";
                script.src = domain + '/webpack-dev-server.js';
                script.onload = resolve;
                document.getElementsByTagName ('body') [0].appendChild(script);
            }),
            new Promise (function (resolve, reject) {
                var script = document.createElement('script');
                script.type = "text\/javascript";
                script.src = domain + '/dist/{{project_name}}.min.js';
                script.onload = resolve;
                document.getElementsByTagName ('body') [0].appendChild(script);
            })
        ]).then(function () {
            new {{library}}.default({{#library_arguments}}{{{library_arguments}}}{{/library_arguments}});
        });

    </script>

</html>